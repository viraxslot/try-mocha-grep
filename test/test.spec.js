const { expect } = require('chai');

describe('suite', function () {
    it('grep test1', function () {
        expect(1).equals(1);
    });

    it('grep test2', function () {
        expect(2).equals(2);
    });
});
